#include "main.h"
/**
 *_abs - finds the absolute value of a number
 *@n: this is the variable checked
 *Return: Always -n (Success), n if otherwise
 */
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
